# 安全政策

## 支持范围

当前只支持最新发布的 `0.1.x` 版本。尚未发布的分支只接受尽力而为的修复。

## 私下报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交，不要创建公开 Issue。报告请包含：

- 受影响版本和浏览器。
- 可重复的操作步骤。
- 可能暴露或损坏的数据。
- 建议修复方式（如果有）。

维护者会先确认收到，再评估影响和修复计划。公开披露前请等待修复版本可用。

## 安全边界

- LifeTally 不需要账号、服务器或远程代码。
- 个人对象和上传壁纸保存在浏览器本地。
- 节假日更新会向公开数据服务发起网络请求。
- 备份文件包含用户内容，不应附在公开 Issue 或 Pull Request 中。
