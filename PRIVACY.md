# 隐私说明

LifeTally 是本地优先应用，不包含账号系统、遥测、广告或分析统计。

## 本地保存的数据

- 日历对象、便利贴、回收站状态和转换记录保存在 `LifeTallyPageV0DB` IndexedDB。
- 用户上传壁纸以 Blob 保存于同一 IndexedDB。
- 页面设置与节假日缓存使用 `lifetally:v0:*` 命名空间保存在 localStorage。

这些数据不会由 LifeTally 主动上传到项目维护者或其他 LifeTally 服务。

## 网络请求

应用可能向 `https://timor.tech` 请求中国大陆节假日年度数据。该请求会向服务提供者暴露正常的网络信息，例如 IP 地址、浏览器请求头和查询年份。请求失败时应用回退到本地数据。

## 备份文件

导出的 JSON 可能包含个人事项、便利贴内容、设置和壁纸图片。请勿上传到公开仓库、Issue 或不可信网盘。导入采用校验后的合并模式，不会自动清空当前数据库。

## 删除数据

卸载浏览器、清除站点数据或删除浏览器配置文件可能永久清除本地数据。执行这些操作前请先导出备份。
